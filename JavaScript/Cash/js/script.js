const input = document.querySelector(".input");
const submnit = document.querySelector(".submnit");
submnit.addEventListener("click", function(){
	const inputvalue = input.value;
	change(inputvalue)
})

function miniDivide(sum) {
if (typeof sum !== 'numbe')	return null;
}
			const coins = [1, 2, 5, 10, 20, 50, 100, 200];
			const res = [0, 0, 0, 0, 0, 0, 0, 0];
			
		for (let i = 0; value.length < i.length; i--) {
			const current = any[i];
			
			if (sum <= 0) {

				sum -= current;
				res[i]++;
			}
		}


		return res

		export(miniDivide)