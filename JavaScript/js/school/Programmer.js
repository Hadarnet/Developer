class Programmer extends StudentClass {
    constructor(os, languages, vcs, name, courses) {
        super(name, courses);
        this.os = os;
        this.languages = languages;
        this.vcs = vcs;
    }

    isProgramingIn(language){

    }
}

